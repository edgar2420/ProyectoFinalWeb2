<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Hamburguesas en <%= restaurante.nombre %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container mt-5">
        <h1>Hamburguesas en <%= restaurante.nombre %></h1>
        <div class="row">
            <% hamburguesas.forEach(hamburguesa => { %>
                <div class="col-md-4">
                    <div class="card mb-4">
                        <img src="<%= hamburguesa.foto %>" class="card-img-top" alt="Foto de la hamburguesa">
                        <div class="card-body">
                            <h5 class="card-title"><%= hamburguesa.nombre %></h5>
                            <p class="card-text"><%= hamburguesa.descripcion %></p>
                            <p class="card-text">Precio: $<%= hamburguesa.precio %></p>
                        </div>
                    </div>
                </div>
            <% }) %>
        </div>
        <a href="/catalogo/restaurantes" class="btn btn-secondary mt-4">Volver a Restaurantes</a>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha1/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
